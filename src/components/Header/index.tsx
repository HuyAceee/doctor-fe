import MenuModal from "components/MenuModal";

const Header = () => {
  return (
    <div className="sticky top-0 w-screen h-20 bg-gray-500 px-10 py-5">
      <MenuModal />
    </div>
  );
};

export default Header;
